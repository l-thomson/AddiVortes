% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.thiessen}
\alias{plot.thiessen}
\title{Trace plots of a fitted model}
\usage{
\method{plot}{thiessen}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"thiessen"}.}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
The per-draw quantities of \code{\link[=thiessen_diagnostics]{thiessen_diagnostics()}} as traces, one panel
per quantity and one line per chain: \code{sigma} where the model has one, the
mean cells per mean tessellation and the mean active covariates per mean
tessellation. Burn-in sweeps are discarded before the first draw is kept,
so a trace shows the kept draws only.
}
\details{
The covariate trace is flat by construction where p is 3 or fewer: the
default \code{omega} of \code{min(3, p)} makes \code{omega / p} equal to 1, so every
tessellation holds every covariate on every draw and the count cannot
move. It is the same property that makes \code{\link[=variable_inclusion]{variable_inclusion()}} uniform
at small p, and it is not a stalled chain.

For traces of the mean function and for distributional displays, pass
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}} to bayesplot: \code{mcmc_trace()} plots sequences,
\code{mcmc_areas()} and \code{mcmc_dens()} plot posterior densities, and
\code{mcmc_combo()} plots both in one figure.
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
control <- thiessen_control(
  tessellations = 10,
  general_params = general_params(burn_in = 20, draws = 40)
)
plot(thiessen(x, y, control, seed = 1))
}
