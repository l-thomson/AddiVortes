% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{term_params}
\alias{term_params}
\title{One ensemble of tessellations}
\usage{
term_params(
  tessellations = NULL,
  k = 3,
  lambda_c = 5,
  geometry = NULL,
  structure = NULL
)
}
\arguments{
\item{tessellations}{Number of tessellations in the ensemble. \code{NULL},
the default, resolves at fit to 200 as \code{mean_params} and to 0 as
\code{variance_params}; a positive count as \code{variance_params} selects the
heteroscedastic model (the paper's count is 40).}

\item{k}{Cell-value prior spread k: sigma_mu = w / (k sqrt(m)) with the
half-width w the outcome family owns (Chipman, George and McCulloch
2010, s. 4). Default 3. The variance ensemble's inverse-gamma cells
do not use it.}

\item{lambda_c}{Cell-count prior rate lambda_c:
b - 1 ~ Poisson(lambda_c). Default 5, following AddiVortes 0.6.8 and
later; the paper reports 25.}

\item{geometry}{The covariate space, from \code{\link[=geometry_params]{geometry_params()}}. \code{NULL},
the default, takes the core's defaults. The ensembles share one
covariate space: set it on \code{mean_params} and it applies to
\code{variance_params} as well.}

\item{structure}{The covariate-inclusion prior, from
\code{\link[=structure_params]{structure_params()}}. \code{NULL} takes the core's defaults. Shared
between the ensembles like \code{geometry}.}
}
\value{
An object of class \code{"term_params"}.
}
\description{
The size, priors and covariate space of one ensemble.
\code{thiessen_control()} takes one as \code{mean_params} and, for the
heteroscedastic model, one as \code{variance_params}.
}
\examples{
term_params(tessellations = 200, lambda_c = 25)
}
\seealso{
\code{\link[=thiessen_control]{thiessen_control()}}, \code{\link[=geometry_params]{geometry_params()}}, \code{\link[=structure_params]{structure_params()}}
}
