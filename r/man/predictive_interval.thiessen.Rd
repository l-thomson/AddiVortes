% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstantools.R
\name{predictive_interval.thiessen}
\alias{predictive_interval.thiessen}
\title{Central posterior predictive interval}
\usage{
\method{predictive_interval}{thiessen}(object, prob = 0.9, newdata = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"thiessen"}.}

\item{prob}{The mass of the interval. Default 0.9.}

\item{newdata}{New covariates, as \code{\link[=predict.thiessen]{predict.thiessen()}} takes them.
\code{NULL}, the default, is the training rows.}

\item{...}{Ignored.}
}
\value{
A double matrix of one row per row of \code{newdata}, with the lower
and upper bounds as columns named by their percentiles.
}
\description{
Central posterior predictive interval
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
control <- thiessen_control(
  tessellations = 10,
  general_params = general_params(burn_in = 20, draws = 40)
)
fit <- thiessen(x, y, control, seed = 1)
head(predictive_interval(fit))
}
