% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{gaussian}
\alias{gaussian}
\title{The Gaussian outcome family}
\usage{
gaussian(nu = 6, q = 0.85)
}
\arguments{
\item{nu}{Degrees of freedom nu of the sigma^2 prior,
sigma^2 ~ nu lambda / chi^2_nu. Default 6. A variance ensemble
requires nu > 2.}

\item{q}{Calibration quantile q of the sigma^2 prior,
Pr(sigma < sigma_hat) = q. Default 0.85.}
}
\value{
An object of class \code{c("thiessen_gaussian", "thiessen_outcome")}.
}
\description{
The Gaussian observation model of Stone and Gosling (2025): one sigma^2
drawn per sweep. Attaching a variance ensemble
(\code{variance_params = term_params(tessellations = ...)} with a positive
count) makes the model heteroscedastic, so the residual variance varies
with x. This function masks \code{\link[stats:gaussian]{stats::gaussian()}} when the package is
attached; the \code{glm} family is still available as \code{stats::gaussian()}.
}
\examples{
gaussian(nu = 3)
}
\seealso{
\code{\link[=probit]{probit()}}, \code{\link[=thiessen_control]{thiessen_control()}}
}
