% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{thiessen_diagnostics}
\alias{thiessen_diagnostics}
\title{Per-draw sampler diagnostics of a fitted model}
\usage{
thiessen_diagnostics(object)
}
\arguments{
\item{object}{An object of class \code{"thiessen"}.}
}
\value{
A data frame with one row per kept draw and the columns \code{chain},
the chain the draw comes from; \code{draw}, its index within that chain;
\code{sigma}, the residual standard deviation, under the Gaussian model
only; \code{cell_count}, the mean cells per mean tessellation; and
\code{dimension_count}, the mean active covariates per mean tessellation.
}
\description{
The trace of the quantities the sampler records once per kept draw. For a
draws object the same quantities, with the mean function, come from
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}}, which \code{bayesplot::mcmc_trace()} takes.
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
fit <- thiessen(x, y, thiessen_control(m = 10, burn_in = 20, draws = 40),
                seed = 1)
head(thiessen_diagnostics(fit))
}
