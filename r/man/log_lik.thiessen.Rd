% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstantools.R
\name{log_lik.thiessen}
\alias{log_lik.thiessen}
\title{Pointwise log-likelihood of a fitted model}
\usage{
\method{log_lik}{thiessen}(object, newdata = NULL, y = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"thiessen"}.}

\item{newdata}{New covariates, as \code{\link[=predict.thiessen]{predict.thiessen()}} takes them.
\code{NULL}, the default, is the training rows.}

\item{y}{The response at \code{newdata}, in the shape \code{\link[=thiessen]{thiessen()}} took:
a \code{Surv} under the AFT and interval-censored families. Taken from
\code{newdata} where the fit came from a formula and \code{newdata} carries the
response column.}

\item{...}{Ignored.}
}
\value{
A double matrix, one row per kept draw and one column per
observation.
}
\description{
The matrix \code{loo::loo()} takes.
}
\examples{
if (requireNamespace("loo", quietly = TRUE)) {
  n <- 60
  x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
  y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
  control <- thiessen_control(
    tessellations = 10,
    general_params = general_params(burn_in = 20, draws = 40)
  )
  fit <- thiessen(x, y, control, seed = 1)
  loo::loo(log_lik(fit))
}
}
