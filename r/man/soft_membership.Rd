% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{soft_membership}
\alias{soft_membership}
\title{Soft membership of observations in cells}
\usage{
soft_membership(rate = 10)
}
\arguments{
\item{rate}{Rate of the exponential prior on the bandwidth, on the
scaled covariate space. Default 10, so the prior mean bandwidth is a
tenth of a column's range.}
}
\value{
An object of class
\code{c("thiessen_soft", "thiessen_membership", "thiessen_option")}, for
the \code{membership} argument of \code{\link[=geometry_params]{geometry_params()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Kernel-weighted membership, the softening of the tree split of Linero
and Yang (2018) carried to the Voronoi assignment: observation i takes
weight proportional to exp(-d^2 / (2 tau^2)) in each cell, normalised
over the tessellation's centres, with tau a per-tessellation bandwidth
under an exponential prior and updated by a Metropolis step. Constant
cell basis and constant spread only. The bandwidth draws are carried
by \code{\link[posterior:as_draws_df]{posterior::as_draws_df()}} as \code{bandwidth[j]}.
}
\section{Experimental}{


This family is compiled only into a core built with its \code{experimental}
Cargo feature. The constructor exists in every build, so a script
naming the family is portable, but a fit or a validated configuration
is rejected with the condition class \code{thiessen_requires_feature}
unless the package was installed from source with
\code{THIESSEN_EXPERIMENTAL=1} in the environment; \code{\link[=core_experimental]{core_experimental()}}
reports the setting of the build in use. An experimental family sits
outside semantic versioning: its configuration and the values it draws
may change in any release. The table of experimental items and their
status is
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/experimental.md}{\code{docs/experimental.md}}.
}

\examples{
\dontshow{if (core_experimental()) withAutoprint(\{ # examplesIf}
geometry_params(membership = soft_membership(rate = 10))
\dontshow{\}) # examplesIf}
}
\references{
Linero, A. R. and Yang, Y. (2018). Bayesian regression tree ensembles
that adapt to smoothness and sparsity. \emph{Journal of the Royal
Statistical Society: Series B} 80(5), 1087-1110.
\doi{10.1111/rssb.12293}
}
\seealso{
\code{\link[=geometry_params]{geometry_params()}}, \code{\link[=core_experimental]{core_experimental()}}
}
