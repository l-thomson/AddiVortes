% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{thiessen}
\alias{thiessen}
\alias{thiessen.default}
\alias{thiessen.data.frame}
\alias{thiessen.formula}
\title{Fit an AddiVortes model}
\usage{
thiessen(x, ...)

\method{thiessen}{default}(x, y, control = thiessen_control(), seed = NULL, chains = 1, ...)

\method{thiessen}{data.frame}(x, y, control = thiessen_control(), seed = NULL, chains = 1, ...)

\method{thiessen}{formula}(
  formula,
  data,
  control = thiessen_control(),
  seed = NULL,
  chains = 1,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of covariates, one row per observation, or a
data frame. A numeric vector is taken as one column.}

\item{...}{Passed to the method.}

\item{y}{The response: a numeric vector of length \code{nrow(x)}, or a
two-level factor. Under the probit model the values must be 0 and 1.}

\item{control}{An object of class \code{"thiessen_control"}, from
\code{\link[=thiessen_control]{thiessen_control()}}.}

\item{seed}{The seed of the chain. \code{NULL}, the default, draws one from
R's stream, so \code{\link[=set.seed]{set.seed()}} governs; a whole number in \verb{[0, 2^53]}
passes to the core unchanged, so the same value reproduces the same
draws for a given package version and platform.}

\item{chains}{The number of chains to run, a whole number. Each chain
has its own seed, derived from \code{seed} in the core, and the draws of the
chains are pooled. Two or more chains give the convergence
diagnostics; one chain does not.}

\item{formula}{A two-sided formula. The left side names the response and
the right side the covariates, \code{.} for every remaining column.}

\item{data}{A data frame holding the columns the formula names.}
}
\value{
An object of class \code{"thiessen"}: a list with the fitted state,
the resolved configuration, the number of chains and of kept draws, the
convergence diagnostics where two or more chains ran, the seed used,
the design, the response, the fitted values, the residuals, the
hardhat blueprint where one applies, and the call.
}
\description{
AddiVortes is Bayesian regression on a sum of Voronoi tessellations
(Stone and Gosling, 2025): the mean function is a sum of m tessellations,
each with a mean per cell, drawn by the Gibbs sampler of the paper. It
stands to BART (Chipman, George and McCulloch, 2010) as a tessellation
stands to a tree: a cell is a region of the covariate space rather than a
box, so a boundary oblique to the axes costs one cell rather than many
splits.
}
\details{
A factor covariate becomes d - 1 treatment-contrast indicators, the first
level as reference, as \code{model.matrix} and upstream AddiVortes encode it.
Where \code{control} declares a \code{metric}, one entry per column, factors are
passed as integer level codes instead and each factor column must declare
\code{"categorical"}.

A factor response must have two levels and becomes 0 and 1 with the first
level as the zero, as \code{glm} treats one.

\code{stats::update()} works on a fit: the call is stored, so
\code{update(fit, seed = 2)} refits with that argument replaced.

With \code{chains} of two or more, the chains are run in turn with the seeds
the core derives from \code{seed}, their draws are pooled, and the fit carries
rank-normalised split R-hat and the bulk and tail effective sample sizes
of sigma and of the mean function at up to twenty training rows
(\code{posterior::summarise_draws()}). A fit warns, and \code{print()} and
\code{summary()} repeat the warning, where R-hat exceeds 1.01 or an effective
sample size falls below 400 (Vehtari and others, 2021). A fit of one
chain says so instead.

Progress over the sweep schedule is signalled with progressr, so a
session reports it after \code{progressr::handlers()} and nothing is printed
by default. The draws do not depend on whether a handler is set.

A fit is a plain R object holding the sampler state, so \code{\link[=saveRDS]{saveRDS()}}
writes one and a later session reads it without a refit. A fit written by
a build with the core's \code{experimental} feature and read by a build
without it errors with the condition class \code{thiessen_error}, naming the
feature, at the first call that needs the state.
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
control <- thiessen_control(
  tessellations = 10,
  general_params = general_params(burn_in = 20, draws = 40)
)
fit <- thiessen(x, y, control, seed = 1)
fit

frame <- data.frame(y = y, a = x[, 1], b = factor(x[, 2] > 0))
thiessen(y ~ a + b, frame, control, seed = 1)
}
\references{
Chipman, H. A., George, E. I. and McCulloch, R. E. (2010). BART: Bayesian
additive regression trees. \emph{The Annals of Applied Statistics} 4(1),
266-298. \doi{10.1214/09-AOAS285}

Stone, A. and Gosling, J. P. (2025). AddiVortes: (Bayesian) additive
Voronoi tessellations. \emph{Journal of Computational and Graphical
Statistics} 34(3), 859-871. \doi{10.1080/10618600.2024.2414104}

Vehtari, A., Gelman, A., Simpson, D., Carpenter, B. and Buerkner, P.-C.
(2021). Rank-normalization, folding, and localization: an improved R-hat
for assessing convergence of MCMC. \emph{Bayesian Analysis} 16(2), 667-718.
\doi{10.1214/20-BA1221}
}
