% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{ordinal_outcome}
\alias{ordinal_outcome}
\title{The ordinal outcome family}
\usage{
ordinal_outcome(categories = NULL, offset = NULL, cutpoint_sd = 1)
}
\arguments{
\item{categories}{Number of ordered categories K, at least 2. \code{NULL},
the default, is the number of levels of the response; a count that
differs from it is an error.}

\item{offset}{The offset c. \code{NULL}, the default, resolves at fit to
Phi^-1 of the share of rows above the first category.}

\item{cutpoint_sd}{Standard deviation of the N(0, cutpoint_sd^2) prior
on the log-gaps between interior cutpoints. Default 1.}
}
\value{
An object of class \code{c("thiessen_ordinal", "thiessen_outcome")}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The ordered probit model of Albert and Chib (1993),
P(y <= k | x) = Phi(gamma_(k+1) - c - f(x)), for an ordered factor
response (the \code{MASS::polr} convention), its levels the categories 0 to
K - 1 in order. The latent variance is fixed at 1 and the first
cutpoint at 0 for identification, and the interior cutpoints are drawn
on the log-gap scale of Albert and Chib (2001). At K = 2 the model is
\code{\link[=probit_outcome]{probit_outcome()}}. \code{\link[=predict]{predict()}} is the expected category on the code
scale, and \code{predict(type = "probs")} the category probabilities with
the levels as column names.
}
\section{Experimental}{


This family is compiled only into a core built with its \code{experimental}
Cargo feature. The constructor exists in every build, so a script
naming the family is portable, but a fit or a validated configuration
is rejected with the condition class \code{thiessen_requires_feature}
unless the package was installed from source with
\code{THIESSEN_EXPERIMENTAL=1} in the environment; \code{\link[=core_experimental]{core_experimental()}}
reports the setting of the build in use. An experimental family sits
outside semantic versioning: its configuration and the values it draws
may change in any release. The table of experimental items and their
status is
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/experimental.md}{\code{docs/experimental.md}}.
}

\examples{
\dontshow{if (core_experimental()) withAutoprint(\{ # examplesIf}
ordinal_outcome(categories = 4)
\dontshow{\}) # examplesIf}
}
\references{
Albert, J. H. and Chib, S. (2001). Sequential ordinal modeling with
applications to survival data. \emph{Biometrics} 57(3), 829-836.
\doi{10.1111/j.0006-341X.2001.00829.x}
}
\seealso{
\code{\link[=probit_outcome]{probit_outcome()}}, \code{\link[=thiessen_control]{thiessen_control()}}, \code{\link[=core_experimental]{core_experimental()}}
}
