% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampler.R
\name{thiessen_sampler}
\alias{thiessen_sampler}
\title{Drive the sampler one call at a time}
\usage{
thiessen_sampler(x, y, control = thiessen_control(), seed = NULL)
}
\arguments{
\item{x}{A numeric matrix of covariates, one row per observation. A
numeric vector is taken as one column.}

\item{y}{The response: a numeric vector of length \code{nrow(x)}. Labels in
\{0, 1\} under the probit family.}

\item{control}{An object of class \code{"thiessen_control"}, from
\code{\link[=thiessen_control]{thiessen_control()}}.}

\item{seed}{The seed. \code{NULL}, the default, draws one from R's stream,
so \code{\link[=set.seed]{set.seed()}} governs; a whole number in \verb{[0, 2^53]} gives the
chain that \code{thiessen()} would run first.}
}
\value{
An object of class \code{"thiessen_sampler"}: an environment holding
the verbs above.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The researcher's interface, after the updatable sampler object of dbarts
and the low-level interface of stochtree: construct with the
configuration, the data and a seed, then drive the Gibbs loop yourself.
Burn-in and thinning are the caller's loop, and the response may be
replaced between sweeps, which is what makes censoring, imputation and
custom likelihoods through the response possible. Anything that is not
an outcome family or a setting goes through this loop.

The response is on the caller's scale through an affine map frozen at
construction, so a response outside the training range is legitimate.
The sampler owns its RNG, seeded at construction with the chain-0 seed
of \code{\link[=thiessen]{thiessen()}}; driving the configured schedule by hand reproduces a
one-chain fit bit for bit. The loop cannot rewire tessellation
membership or cell internals. The \code{burn_in}, \code{draws} and \code{thinning}
settings of the control play no part here.

The returned object holds the loop's verbs:
\describe{
\item{\verb{$step(n = 1)}}{Run \code{n} sweeps of the Gibbs loop.}
\item{\verb{$keep()}}{Record the current state as a posterior draw.}
\item{\verb{$n_kept()}}{The number of draws kept so far.}
\item{\verb{$set_response(y)}}{Replace the response, keeping the
tessellations, the cell values and sigma^2; the next sweep
conditions on it. Labels in \{0, 1\} under the probit family.}
\item{\verb{$fitted_values()}}{The current mean function at the training
rows: f(x_i), or c + f(x_i) under the probit family.}
\item{\verb{$noise_variances()}}{The current variance of y given f at each
training row: sigma^2 under the Gaussian model, 1 under the probit
family (the latent scale), s^2(x_i) under the heteroscedastic
model.}
\item{\verb{$finish()}}{The fit of the kept draws, as \code{\link[=thiessen]{thiessen()}} returns
one. Consumes the sampler: every later call on it errors.}
}
}
\examples{
fixture <- matrix(seq(0, 1, length.out = 40), ncol = 1)
response <- 3 * fixture[, 1]^2 - fixture[, 1]

sampler <- thiessen_sampler(fixture, response,
                            thiessen_control(tessellations = 10),
                            seed = 1)
sampler$step(20)
for (draw in seq_len(30)) {
  sampler$step(1)
  sampler$keep()
}
fit <- sampler$finish()
fit$n_draws
}
\seealso{
\code{\link[=thiessen]{thiessen()}}, whose loop this is.
}
