% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{interval_censored_outcome}
\alias{interval_censored_outcome}
\title{The interval-censored outcome family}
\usage{
interval_censored_outcome(nu = 6, q = 0.85)
}
\arguments{
\item{nu}{Degrees of freedom nu of the sigma^2 prior,
sigma^2 ~ nu lambda / chi^2_nu. Default 6. A variance ensemble
requires nu > 2.}

\item{q}{Calibration quantile q of the sigma^2 prior,
Pr(sigma < sigma_hat) = q. Default 0.85.}
}
\value{
An object of class
\code{c("thiessen_interval_censored", "thiessen_outcome")}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The interval-censoring observation scheme (Sun 2006) for a response
known only to lie between two row-specific bounds, an equal pair being
an exact value and an infinite endpoint one-sided censoring. The
censoring is taken as independent of the response, so the bounds enter
the likelihood only through the interval probability.

The bounds are data, not parameters: the response is a
\code{\link[survival:Surv]{survival::Surv()}} of type \code{"interval2"}, \code{Surv(lower, upper, type = "interval2")}, in which an \code{NA} bound is one-sided censoring and an
equal pair an exact value; it selects this family by itself.
\code{\link[=predict]{predict()}} and the fitted values are the uncensored f(x), and
\code{\link[=log_lik]{log_lik()}} is the interval likelihood of a \code{Surv} response.
}
\section{Experimental}{


This family is compiled only into a core built with its \code{experimental}
Cargo feature. The constructor exists in every build, so a script
naming the family is portable, but a fit or a validated configuration
is rejected with the condition class \code{thiessen_requires_feature}
unless the package was installed from source with
\code{THIESSEN_EXPERIMENTAL=1} in the environment; \code{\link[=core_experimental]{core_experimental()}}
reports the setting of the build in use. An experimental family sits
outside semantic versioning: its configuration and the values it draws
may change in any release. The table of experimental items and their
status is
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/experimental.md}{\code{docs/experimental.md}}.
}

\examples{
\dontshow{if (core_experimental()) withAutoprint(\{ # examplesIf}
interval_censored_outcome()
\dontshow{\}) # examplesIf}
}
\references{
Sun, J. (2006). \emph{The Statistical Analysis of Interval-censored Failure
Time Data}. Springer. \doi{10.1007/0-387-37119-2}
}
\seealso{
\code{\link[=thiessen_control]{thiessen_control()}}, \code{\link[=core_experimental]{core_experimental()}}
}
