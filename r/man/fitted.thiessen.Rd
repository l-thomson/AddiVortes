% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{fitted.thiessen}
\alias{fitted.thiessen}
\alias{residuals.thiessen}
\alias{nobs.thiessen}
\title{Fitted values, residuals and observation count of a fitted model}
\usage{
\method{fitted}{thiessen}(object, ...)

\method{residuals}{thiessen}(object, ...)

\method{nobs}{thiessen}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"thiessen"}.}

\item{...}{Ignored.}
}
\value{
For \code{fitted()} and \code{residuals()}, a numeric vector of length
\code{nobs(object)}: the posterior mean of the response at each training row,
and the response less that mean. For \code{nobs()}, the number of
observations.
}
\description{
Fitted values, residuals and observation count of a fitted model
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
fit <- thiessen(x, y, thiessen_control(m = 10, burn_in = 20, draws = 40),
                seed = 1)
nobs(fit)
head(residuals(fit))
}
