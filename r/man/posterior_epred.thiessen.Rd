% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstantools.R
\name{posterior_epred.thiessen}
\alias{posterior_epred.thiessen}
\title{Draw from the posterior distribution of the expected response}
\usage{
\method{posterior_epred}{thiessen}(object, newdata = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"thiessen"}.}

\item{newdata}{New covariates, as \code{\link[=predict.thiessen]{predict.thiessen()}} takes them.
\code{NULL}, the default, is the training rows.}

\item{...}{Ignored.}
}
\value{
A double matrix, one row per kept draw and one column per row of
\code{newdata}: the mean of the response, the probability under the probit
model.
}
\description{
Draw from the posterior distribution of the expected response
}
\examples{
n <- 60
x <- cbind(seq(0, 1, length.out = n), rep(c(0, 0.5), length.out = n))
y <- 2 * (x[, 1] - 0.5)^2 + 0.5 * x[, 2]
fit <- thiessen(x, y, thiessen_control(m = 10, burn_in = 20, draws = 40),
                seed = 1)
dim(posterior_epred(fit))
}
