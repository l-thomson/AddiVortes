% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{thiessen_control}
\alias{thiessen_control}
\title{Configuration of a fit}
\usage{
thiessen_control(
  outcome = gaussian_outcome(),
  mean_params = term_params(),
  variance_params = NULL,
  general_params = NULL,
  tessellations = NULL
)
}
\arguments{
\item{outcome}{The outcome family, from \code{\link[=gaussian_outcome]{gaussian_outcome()}},
\code{\link[=probit_outcome]{probit_outcome()}} or one of the experimental constructors above.}

\item{mean_params}{The ensemble describing the average, from
\code{\link[=term_params]{term_params()}}.}

\item{variance_params}{The ensemble describing the spread, from
\code{\link[=term_params]{term_params()}}. \code{NULL}, the default, keeps the spread constant.}

\item{general_params}{The sweep schedule, from \code{\link[=general_params]{general_params()}}.
\code{NULL}, the default, is \code{general_params()}.}

\item{tessellations}{Shortcut for the mean ensemble's size:
\code{thiessen_control(tessellations = 200)} is
\code{thiessen_control(mean_params = term_params(tessellations = 200))}.
An error if \code{mean_params} also sets a count.}
}
\value{
An object of class \code{"thiessen_control"} holding the four
groups.
}
\description{
The configuration of Stone and Gosling (2025), s. 2, in the shape the
core stores it: an outcome family, one parameter group per ensemble,
and the sweep schedule. Each part has its own constructor with its own
documentation: \code{\link[=gaussian_outcome]{gaussian_outcome()}} and \code{\link[=probit_outcome]{probit_outcome()}} for the family,
\code{\link[=term_params]{term_params()}} for an ensemble, and \code{\link[=general_params]{general_params()}} for the
schedule. An argument left at its default gives the core's default, so
\code{thiessen_control()} is the published configuration.
}
\details{
Attaching \code{variance_params} with a positive tessellation count selects
the heteroscedastic model, in which the residual variance varies with
x; it needs the Gaussian family with nu > 2, and the paper's count is
40. The two ensembles share one covariate space: \code{geometry} and
\code{structure} set on \code{mean_params} apply to \code{variance_params} as well.

One shortcut: \code{thiessen_control(tessellations = 200)} sets the mean
ensemble's size without spelling the group, since that count is the
single number most fits tune. Every other setting is named in its
group.

The models reachable with \code{\link[=gaussian_outcome]{gaussian_outcome()}} and \code{\link[=probit_outcome]{probit_outcome()}}
are the published method and follow semantic versioning. Everything
else the core crate adds sits behind its \code{experimental} Cargo feature,
which a released build does not enable, so a configuration or a saved
fit naming such an option is rejected with the condition class
\code{thiessen_requires_feature}. The families it gates each have a
constructor here (\code{\link[=tobit_outcome]{tobit_outcome()}}, \code{\link[=aft_outcome]{aft_outcome()}},
\code{\link[=interval_censored_outcome]{interval_censored_outcome()}}, \code{\link[=ordinal_outcome]{ordinal_outcome()}},
\code{\link[=student_t_outcome]{student_t_outcome()}} and \code{\link[=laplace_outcome]{laplace_outcome()}}), so naming one is
portable, and a build accepting them is installed from source with
\code{THIESSEN_EXPERIMENTAL=1} in the environment; \code{\link[=core_experimental]{core_experimental()}}
reports the setting of the build in use. Each item graduates on its own
once calibrated and is then accepted as any other option, with no
separate opt-in. The table of experimental items and their status is
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/experimental.md}{\code{docs/experimental.md}}.

The core's calibration suite covers the configurations listed in
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/calibrated.md}{\code{docs/calibrated.md}};
component options are verified in isolation, and every other combination
of the documented options is valid to run and is not separately
verified.
}
\examples{
thiessen_control(tessellations = 50)

thiessen_control(
  outcome = gaussian_outcome(nu = 10),
  mean_params = term_params(tessellations = 200, lambda_c = 25),
  variance_params = term_params(tessellations = 40),
  general_params = general_params(burn_in = 500, draws = 2000)
)
}
\references{
Stone, A. and Gosling, J. P. (2025). AddiVortes: (Bayesian) additive
Voronoi tessellations. \emph{Journal of Computational and Graphical
Statistics} 34(3), 859-871. \doi{10.1080/10618600.2024.2414104}
}
