% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{aft_outcome}
\alias{aft_outcome}
\title{The accelerated failure time outcome family}
\usage{
aft_outcome(nu = 6, q = 0.85)
}
\arguments{
\item{nu}{Degrees of freedom nu of the sigma^2 prior,
sigma^2 ~ nu lambda / chi^2_nu. Default 6. A variance ensemble
requires nu > 2.}

\item{q}{Calibration quantile q of the sigma^2 prior,
Pr(sigma < sigma_hat) = q. Default 0.85.}
}
\value{
An object of class \code{c("thiessen_aft", "thiessen_outcome")}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The lognormal accelerated failure time model (Wei 1992) for a
right-censored time to event, the model of the BART package's \code{abart}:
ln T = f(x) + e with e ~ N(0, sigma^2), the log time of a censored row
drawn from its truncated conditional before each sweep.

The times and the event indicator are data, not parameters: the
response is a \code{\link[survival:Surv]{survival::Surv()}} of type \code{"right"}, which selects this
family by itself. \code{\link[=predict]{predict()}} and the fitted values are f(x) on the
log-time scale, the \code{abart} convention, and \code{\link[=log_lik]{log_lik()}} is the
censored likelihood of a \code{Surv} response.
}
\section{Experimental}{


This family is compiled only into a core built with its \code{experimental}
Cargo feature. The constructor exists in every build, so a script
naming the family is portable, but a fit or a validated configuration
is rejected with the condition class \code{thiessen_requires_feature}
unless the package was installed from source with
\code{THIESSEN_EXPERIMENTAL=1} in the environment; \code{\link[=core_experimental]{core_experimental()}}
reports the setting of the build in use. An experimental family sits
outside semantic versioning: its configuration and the values it draws
may change in any release. The table of experimental items and their
status is
\href{https://github.com/l-thomson/thiessen/blob/dev/docs/experimental.md}{\code{docs/experimental.md}}.
}

\examples{
\dontshow{if (core_experimental()) withAutoprint(\{ # examplesIf}
aft_outcome()
\dontshow{\}) # examplesIf}
}
\references{
Wei, L. J. (1992). The accelerated failure time model: a useful
alternative to the Cox regression model in survival analysis.
\emph{Statistics in Medicine} 11(14-15), 1871-1879.
\doi{10.1002/sim.4780111409}
}
\seealso{
\code{\link[=thiessen_control]{thiessen_control()}}, \code{\link[=core_experimental]{core_experimental()}}
}
