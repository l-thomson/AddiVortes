% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{cell_params}
\alias{cell_params}
\title{The within-cell response surface of the ensembles}
\usage{
cell_params(basis = NULL)
}
\arguments{
\item{basis}{The value a cell holds: \code{"constant"}, one value per cell,
the published basis; or \code{"linear"}, a value that tilts across the
cell, mu + beta' (x_A - c) over the active covariates centred at the
cell's centre, with the slopes under the cell-value prior. The linear
basis is compiled only into a core built with its \code{experimental}
feature (see \link{experimental_outcomes} for the policy), needs every
column min-max scaled, and applies to the mean ensemble only. \code{NULL},
the default, is \code{"constant"}.}
}
\value{
An object of class \code{"cell_params"}.
}
\description{
The within-cell response surface of the ensembles
}
\examples{
cell_params(basis = "constant")
}
\seealso{
\code{\link[=term_params]{term_params()}}
}
